/*************************************************************************
	> File Name: zuihouyigeshuzi.c
	> Author: 
	> Mail: 
	> Created Time: Fri 21 Apr 2017 07:32:57 PM PDT
 ************************************************************************/

#include<stdio.h>
//单独的数字
#include<stdlib.h>
/*int Select(int a[], int len)
{
    int r = 0;
    int s[32] = {0};

    for(int i = 0; i < len; ++i)
    {
        for(int j = 0; j < 32; ++j)
        {
            s[j] = (s[j] + (a[i]>>j&1))%3;
           // printf("%d|", s[j]);
        }
    }

    for(int i = 0; i < 32; ++i)
    {
        r +=  (s[i] << i);
    }
    return r;
}*/

/*int main()
{
    int n;
    printf("请输入数组长度：\n");
    scanf("%d", &n);
    int a[n];
    
    printf("请输入数组元素：\n");
    for(int i = 0; i < n; ++i)
    {
        scanf("%d", &a[i]);
    }
    int a[10] = {2, 2, 2, 4, 4, 4, 5, 5,5, 6};
    printf("单独的数字：%d\n", Select(a,4));
    return 0;
}*/
/*int main()
{
    int *a;
    int len;

    printf("请输入数组长度：\n");
    scanf("%d" ,&len);

    a = (int *)malloc(sizeof(int) * len);

    printf("请输入数组元素：\n");
    for(int i = 0; i < len; ++i)
    {
        scanf("%d", &a[i]);
    }

    int i = 0, m, j;
    int count = 0;
    for(int i = 0; i < len; ++i)
    {
            
        
    }
    printf("单独的数字：%d\n",m);
    return 0;
}*/

 /*   FILE *fp;

    int a[20];

    if((fp = fopen("/home/lyzgit/JSKquestions/testcount", "rt")) == NULL)
    {
        printf("打开失败！");
        exit(1);
    }*/
